# Task: About Page

**Branch:** `feat/page-about`

## Scope of Work & Creative Direction
This branch tells the story of HWI JDCOEM.

### 1. Vertical Timeline
- Build a vertical timeline component based on data from `src/data/timeline.ts`.
- Because timeline content is genuinely sequential, a numbered or stepped visual treatment is earned and appropriate here (unlike using numbers as generic decoration on other pages).
- Adhere strictly to the typography and layout rules defined in the Design Tokens (left-aligned, proper line length).

## ⚠️ Zero Merge Conflict Rules (CRITICAL)
1. **Strict File Boundaries:** Only edit `app/about/page.tsx`, `components/about/*`, and `data/timeline.ts`.
2. **No Dependency Changes:** Do NOT run `npm install` or modify `package.json`.
3. **No Drive-by Fixes:** Do not fix code outside your scope.
4. **Stay Updated:** Run `git fetch origin` and `git merge origin/develop` frequently.
